name: magicformula-env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pandas>=2.0.0
  - numpy>=1.24.0
  - pip
  - pip:
      - yfinance>=0.2.28
      - pydantic>=2.0.0
      - pydantic-settings>=2.0.0
      - python-dateutil>=2.8.2
